"""Log ingestion.

Will read raw multi-line logs from a file path or stdin and normalize them into
the plain text the triage step consumes (no parsing/interpretation here).

See docs/TRD.md (ingest -> triage -> emit contract).
"""

# TODO(phase-ingestion): read raw logs from path/stdin and return normalized text.
