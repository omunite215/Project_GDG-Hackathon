"""Emit the validated result.

Will serialize the validated TriageResult to the EXACT 4-field JSON object and
write it to stdout (and/or POST it to a webhook).

See docs/PRD.md (output contract).
"""

# TODO(phase-orchestration): serialize TriageResult to JSON for stdout/webhook.
